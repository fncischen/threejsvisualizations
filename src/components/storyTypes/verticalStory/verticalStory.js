export default class verticalStory {
    constructor(props){
        this.characters = props.characters;
        this.environments = props.environments;
        this.storyIndex = props.storyIndex;  
        this.storyUI = props.storyUI;
    }

    startAnimationsForCharacters(){

    }

    startStoryUI(){
        
    }

}