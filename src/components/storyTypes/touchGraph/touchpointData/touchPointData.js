
// abstract class which handles all the touchpoint related data and handles how its organized;
export default class touchPointData {
    constructor(props){
        this.name = props.name;
        this.dataType = props.dataType; 
        
        // this could be 
        // we assign weights to these data elements via graphical, front-end data points
        // and information design 
    }

}